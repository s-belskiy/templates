import { Box } from '@mui/material';
import AppLayout from '../../../layouts/AppLayout';

export default function IdeaStatuses(props: {}) {
	return (
		<AppLayout title={'Статусы идей'}>
			<Box>Статусы идей</Box>
		</AppLayout>
	);
}
