import { Box } from '@mui/material';
import AppLayout from '../../../layouts/AppLayout';

export default function Tags(props: {}) {
	return <AppLayout title={'Теги (технологии)'}>
  <Box>Теги (технологии)</Box>
</AppLayout>;
}
